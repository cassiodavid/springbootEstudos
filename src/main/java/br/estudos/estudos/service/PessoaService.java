package br.estudos.estudos.service;

//import java.util.ArrayList;
//import java.util.List;

//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.data.domain.Page;
//import org.springframework.data.domain.PageRequest;
//import org.springframework.data.domain.Pageable;
//import org.springframework.data.domain.Sort;

//import br.estudos.estudos.entity.Pessoa;
//import br.estudos.estudos.repository.PessoaRepository;

public class PessoaService {

    /*
     * @Autowired private PessoaRepository _pessoaRepository;
     * 
     * public List<Pessoa> getAllPessoas(Integer pageNo, Integer PageSize, String
     * sortBy) {
     * 
     * Pageable paging = PageRequest.of(pageNo, PageSize, Sort.by(sortBy));
     * Page<Pessoa> pagedResult = _pessoaRepository.findAll(paging);
     * 
     * if (pagedResult.hasContent()) { return pagedResult.getContent();
     * 
     * } else { return new ArrayList<Pessoa>(); } }
     */

}
